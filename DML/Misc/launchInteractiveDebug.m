function launchInteractiveDebug(input)
%LAUNCHINTERACTIVEDEBUG Matlab prompt, with a single variable (given) variable in the workspace; convenient debugging for calling from a MEX file, for example.

keyboard();
